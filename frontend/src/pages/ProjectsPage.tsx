import { Container, Stack, Text, Title } from "@mantine/core";

/** Projects page (T9). Placeholder until the projects feature lands. */
const ProjectsPage = () => (
  <Container size="lg">
    <Stack gap="sm">
      <Title order={2}>Projects</Title>
      <Text c="dimmed">Lands in a later phase (T9).</Text>
    </Stack>
  </Container>
);

export default ProjectsPage;
